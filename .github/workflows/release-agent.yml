name: Release Agent

on:
    workflow_dispatch:
        inputs:
            release_plan:
                description: "Release plan (provided by Tagline bot)"
                required: true
                type: string
            issue_number:
                required: false
                type: string
                default: ""
            dry_run:
                required: false
                type: boolean
                default: false

jobs:
    release:
        runs-on: ubuntu-latest
        permissions:
            contents: write
            pull-requests: write
            issues: write
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
                  token: ${{ secrets.GITHUB_TOKEN }}

            - uses: moeen-mahmud/tagline-release-agent-action@main
              with:
                  release_plan: ${{ inputs.release_plan }}
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  issue_number: ${{ inputs.issue_number }}
                  dry_run: ${{ inputs.dry_run }}
